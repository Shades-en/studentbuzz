import { Outlet } from "react-router-dom";

const Navbar = () => {
    return (  
        <>
            <nav> navbar student </nav>
            <Outlet />
        </>
    );
}
 
export default Navbar;